# Контент и перелинковка

Работа 27–31 июля. Восстановлено из транскрипта сессии.

## Статьи

- Объединены две статьи про красный чай, дублировавшие друг друга.
- Исправлен уродливый слаг `/14762-2/` у статьи про шу пуэр.
- Из статей проставлены ссылки на реальные товары в наличии: красный чай,
  шу пуэр, матча, лю бао, улун.
- Починены битые ссылки на категории — они вели в никуда и убивали конверсию.

## Категории

- Восстановлено описание категории на архиве (терялось из-за темы).
- У зелёного чая исправлены параметры заваривания и удалён несуществующий сорт.
- У белого чая параметры исправлены позже, 3 августа — см. [[Блог и SEO]].

## Картинки

Проведена сплошная проверка alt-текстов по каталогу.

## Отзывы

Страница `/otzyvy/` переделана: подставлены настоящие отзывы из Яндекс.Карт и
Google Карт вместо выдуманных, починена битая ссылка.

Связано: [[Блог и SEO]], [[SEO — аудит и мета-описания]]

## Обратная перелинковка: товары → статьи

Ссылки из статей на товары были проставлены раньше, **обратных не было вовсе** —
на карточке товара ноль ссылок на блог. Посетитель, пришедший на товар, мог уйти
только в другой товар или с сайта. Прямая причина низкой глубины просмотра.

Сниппет **149** добавляет блок «Разобраться в этом чае» — 2–3 ссылки на статьи,
подобранные по категории товара. Для посуды — гайвань, исинский чайник, гунфу ча.
Если категория не в списке, даётся общий вход: виды китайского чая и температура воды.

### Грабли: карточку строит Elementor

`woocommerce_after_single_product_summary` на карточке **не вызывается** — шаблон
собран Elementor (активны ShopEngine и WooLentor). Обработчик регистрировался,
но не срабатывал.

Проверка хуков показала, что работают `woocommerce_product_meta_end`,
`wp_footer` и `woocommerce_after_single_product`, а `the_content` и
`woocommerce_single_product_summary` — нет. Блок повешен на
`woocommerce_product_meta_end`.

> Ссылки отрисовываются сервером, а не скриптом — иначе поисковик мог бы их
> не учесть, а весь смысл правки в перелинковке.

Ещё одно наблюдение оттуда же: строки `related` и `upsell` в исходном коде
страницы есть, но это имена CSS-классов, а не блоки. Стандартных блоков
похожих товаров на карточке нет.

## Похожие товары на карточке (сниппет 153)

Штатного блока похожих товаров на карточке **не было вовсе** — слова `related`
и `upsell` в коде страницы это имена CSS-классов, а не разметка. Сделан свой.

Грабли: первая версия подбирала по всем категориям товара, а товар состоит ещё и
в каталоге-свалке «Основной каталог» (id 15, 333 позиции). К красному чаю
подтягивались пиала и чайная доска. Теперь берётся самая узкая категория,
свалка исключена.

Вторая мелочь: названия обрезались до 7 слов, и позиции «ШФ — 1 категория» и
«ШФ — 2 категория» выглядели дублями. Обрезку убрал, ограничил тремя строками CSS.

## Похожие статьи в конце записи (сниппет 154)

Блок «Что почитать дальше» — три статьи, подобранные по близости темы.

### Три ошибки подбора, каждую пришлось искать отдельно

1. **Блок не выводился.** Условие включало `in_the_loop()`, а содержимое статьи
   рисует виджет Elementor **вне основного цикла** — проверка всегда давала false.
   Диагностика: отладочные метки на `the_content`, `wp_footer` и
   `elementor/frontend/after_render` показали, что фильтр срабатывает,
   но `loop=0`.
2. **Подбор был случайным.** При нехватке записей по меткам добор шёл `orderby=rand`,
   и к шу пуэру подставлялась история красного чая. Заменено на оценку совпадений
   значимых слов заголовка.
3. **Совпадений не находилось из-за морфологии.** «Белый» и «белого» — разные
   строки. Добавлено отсечение окончаний, теперь оба дают «бел».
4. **Слаги давали ложные совпадения.** В сравнение шли и заголовок, и слаг, а
   подстрока `chaj` есть почти в каждом слаге — совпадение находилось у всех
   подряд, и решала уже дата. Сравниваются только заголовки.

Метки понижены до тай-брейка: они общие («чай»), и складываясь со смысловым весом
перебивали его.

> Идеального попадания нет и не будет без ручной разметки: у одиночных тем
> (ГАБА, исинский чайник) соседей просто нет. Для основных категорий подбор верный.

## Описания товаров не выводились вообще

**Самая крупная находка этого дня.** Владелец сказал, что у новых товаров нет
описаний. Проверка базы показала обратное: без описания был **1 товар из 375**,
у остальных тексты на 700–2700 символов, включая свежие от 29 июля.

Но на странице описания **не было**. Проверено прямо: текст из `post_content`
не встречается в HTML карточки, вкладок `woocommerce-Tabs` нет. Шаблон карточки
собран Elementor и описание не выводит.

То есть тексты у 374 товаров были написаны, но их не видели ни покупатели, ни
поисковики. Это объясняет и низкое время на сайте — на карточке нечего читать, —
и то, почему владельцу казалось, что описаний нет.

Исправлено сниппетом **161**: описание выводится на `woocommerce_after_single_product`.

### Порядок блоков и почему один сниппет вместо трёх

Сначала описание, похожие товары и ссылки на статьи жили в трёх сниппетах.
Порядок вывода получался непредсказуемым: приоритеты хука складывались не так,
как ожидалось, и описание оказывалось последним, ниже вспомогательных блоков.
Несколько попыток задать приоритеты результата не дали.

Решение: **свести в один сниппет** и вызывать блоки подряд из одного обработчика.
Порядок задан явно и не зависит от того, в каком порядке грузятся сниппеты:
описание → похожие чаи → что почитать.

> Урок: когда порядок вывода важен, не стоит распределять его по приоритетам
> разных сниппетов. Один обработчик надёжнее.

### Мои ошибки по ходу

- Заявил владельцу, что у товаров пустые описания. **Неправда** — пустое было одно.
  Ошибка родилась раньше: мой шаблон поиска описания на карточке вернул «0 слов»,
  но провалился сам замер, а я принял это за факт.
- Первая попытка записать описание через WooCommerce API вернула 200, но ничего
  не сохранила: отправил поле `content` вместо `description`.
