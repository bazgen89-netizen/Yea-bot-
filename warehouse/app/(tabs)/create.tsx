import { Redirect } from 'expo-router';

/**
 * Заглушка под центральную кнопку нижней панели.
 *
 * Кнопка «+» не открывает вкладку — она поднимает шторку создания, и нажатие
 * перехвачено в разметке вкладок. Экран нужен только для того, чтобы у кнопки
 * было своё место в панели; если сюда всё же попали, отправляем на главную.
 */
export default function CreateTabPlaceholder() {
  return <Redirect href="/" />;
}
