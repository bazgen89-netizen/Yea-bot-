import { Section } from '../src/web/screens/Section';

export default function CompanyScreen() {
  return <Section title={'Настройки / настройки компании'} note={'Наименование, страна, валюта, реквизиты, налоги и печатные формы.'} />;
}
