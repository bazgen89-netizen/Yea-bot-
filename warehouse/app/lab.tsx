import { Section } from '../src/web/screens/Section';

export default function LabScreen() {
  return <Section title={'Лаборатория'} note={'Возможности, которые пробуются до того, как станут обычными разделами.'} />;
}
