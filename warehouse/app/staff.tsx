import { Section } from '../src/web/screens/Section';

export default function StaffScreen() {
  return <Section title={'Компания / сотрудники'} note={'Сотрудники, их роли и права: владелец видит всё, продавец — только продажи.'} />;
}
