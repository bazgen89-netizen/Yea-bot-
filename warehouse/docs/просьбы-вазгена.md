# Просьбы Вазгена: что сделано и что осталось

Единственный список задач по этой программе. Сюда попадает **всё**, о чём он
попросил, — в тот же ответ, дословной строкой и с датой. Порядок работы описан
в `.claude/skills/задачи-вазгена/SKILL.md`.

Обозначения: **[x]** — сделано и проверено в собранном приложении ·
**[ ]** — не сделано · **[?]** — ждём от него снимок, токен или ответ.

---

## Сделано

- [x] 20.08 · «Движение денег» и «История бонусов» в карточке клиента — были
  пустыми заглушками. Деньги: приходы по его чекам, карточками по дням.
  Бонусы: начисление и списание лежат **в самих чеках** (`bonus_cashback`,
  `bonus_spent`, `bonus_discount` построчно) — забрал их и веду счёт по
  покупкам: «Покупка / Начислено / Списано / Остаток». Отдельной таблицы
  бонусных операций CloudShop по ключу не отдаёт вовсе — проверил семь
  возможных адресов, все 404.

- [x] 19.08 · **Найдено, куда девались данные.** Он показал карточку Анны
  Швецовой: 16 продаж, 12 181,30, средний 761,33. В переносе всё это лежало
  **точности той же** — теряла загрузка в программу. Чек, в котором не нашлось
  ни одного товара из нынешнего каталога, не заводился вовсе: так пропадали
  **4004 чека на 1 706 708,98** и 26 353 строки. Товары в них не выдуманные —
  просто давно сняты с продажи. Теперь такие заводятся в архив (в справочнике
  и на витрине их нет, продать нельзя), а чек остаётся целым. Проверено:
  карточка Анны совпала с его снимком до копейки, включая три позиции чека
  #5532 с количествами 30,5 / 30,5 / 30,7.
- [x] 19.08 · Карточка клиента по его снимкам: статистика двумя колонками с
  точками — долг по продажам и возвратам, количество продаж и возвратов,
  суммы, приходы, расходы, средний чек, баланс. Чек раскрывается: таблица
  «Наименование / Кол-во / Цена / Итог».

- [x] 19.08 · **Сутки и часы — по времени магазина, а не по Гринвичу.** Он
  сказал «данные не верные» — и был прав. Время чека хранится по Гринвичу, а
  сутки резались прямо по строке: чек, пробитый в половине первого ночи,
  уезжал во вчерашнюю выручку, а график часов показывал 7 утра там, где
  торговали в 10. Теперь перед тем как отрезать день или час, время сдвигается
  к местному — сдвиг берётся у браузера, потому что в SQLite на странице
  справочника часовых поясов нет. Есть проверка на ночной чек.
- [x] 19.08 · История покупок в карточке клиента — по его снимку: заголовок
  дня («19 августа»), карточки с цветной полосой слева, «Продажа #45860» и
  сумма справа, «Магазин … ❯ клиент …», магазин со временем, «Документ
  оплачен» и число позиций. Было — строка «дата, название, сумма».

- [x] 19.08 · Выбор магазина в телефоне — по его снимку: лист снизу, поиск,
  название с адресом второй строкой, кружок справа. Адреса приезжают из
  CloudShop; «магазинов» он отдаёт семь, но три из них — счета («№1»,
  «Терминал / Счет в банке») и старые двойники, у настоящих есть адрес.
- [x] 19.08 · Светлая и тёмная тема — по настройке телефона («у меня после
  заката всё меняется со светлого на тёмный»). В браузере цвета вынесены в
  переменные оформления и меняются вместе с системной темой, не перезапуская
  программу. Проверено на телефонном экране в обеих.
- [x] 19.08 · График дня — по часам. За один день выходила одна точка, и
  столбик растягивался синим прямоугольником во всю карточку. Теперь часы с
  подписями и серой дорожкой, как у него.

- [x] 19.08 · Выбор магазина на главной телефона — тот значок лавки, что он
  обвёл. По нему пересчитывается всё: выручка, себестоимость, прибыль,
  средний чек, график по дням, остаток и его стоимость; выбранный магазин
  подписан в шапке. Отсюда и «странные данные»: его телефон показывал один
  магазин (Черёмушки, ~12 930 за день), а наша главная — все семь сразу
  (13 880 за тот же день). Данные были верны, не хватало выбора.
- [x] 19.08 · Ссылка — с **настоящими** контактами, по его прямой просьбе
  («мне всё равно новый или старый, главное чтобы был рабочий»,
  «чтобы были реальные контакты с данными, всеми данными и историей»).
  `node scripts/build-demo.mjs --real`. Вопрос про старый коммит с
  телефонами закрыт: он ответил, что это неважно.

- [x] 19.08 · Клиенты: «всё криво, нет всех клиентов с их информацией».
  Снимка он не прислал, сказал «сделай сам» — сделал по тому, что видно.
  Половину ширины занимали «Описание» и «Адрес», пустые у всех 3209 карточек
  (CloudShop этих полей по ключу не отдаёт вовсе), и из-за них скидка с
  бонусами уезжали за правый край — в наборе «Лояльность», куда никто не
  заглядывает. Теперь: колонка, пустая у всех, не показывается — и вернётся
  сама, как только значение появится хоть у одного; скидка и бонусы стоят в
  основном наборе. «Добавил: CloudShop» убрано — кто завёл карточку, они не
  сообщают, а я заполнял этим словом три тысячи строк. Таблица влезает в
  экран целиком: имя, телефон, почта, скидка, бонусы, день рождения, пол,
  дата заведения.
- [x] 19.08 · Ссылка для просмотра — вместе с файлом, каждый раз:
  https://claude.ai/code/artifact/87a7e100-421b-47d3-a821-43f2b9a1fb85 —
  «файлы почему-то не открываются». По ссылке имена и телефоны выдуманные
  (страница живёт на чужом сервере) и история короче — 13 365 свежих чеков
  вместо 45 769: страница по ссылке не может быть тяжелее 16 МБ.

- [x] 19.08 · Раздел «Оборудование» в настройках кассы по его снимкам: карточка
  «Возможности Windows кассы», «Безналичные» с четырьмя интеграциями (Яндекс
  Пэй, Сбербанк, Альфа-Банк, Mertech QR) и «Диагностика оборудования».
  Проверка сканера работает по-настоящему: штрихкод ищется в справочнике, код
  маркировки разбирается на код товара и номер экземпляра. Весы честно
  подписаны «не подключены» — из браузера кабель недоступен.
- [x] 18.08 · Тёмная тема: сперва сделал по палитре их нового приложения —
  вышла чёрная и не та; переделана по его снимку в тёмно-синюю (подложка
  `#1B2436`, карточки `#2A3446`, шапка окна `#3F4B60`). Проверена на витрине,
  в настройках и в журнале чеков.
- [x] 18.08 · Возврат продажи: полоса внизу оранжевая и подписана
  «ВОЗВРАТ ПРОДАЖИ» — как на его снимке.
- [x] 19.08 · **Перенос из CloudShop выполнен** по ключу Connect API:
  590 товаров (449 с фотографиями, со штрихкодами, артикулами, единицами,
  ценами, описаниями и остатками по семи магазинам) и 3206 клиентов
  (613 со скидками, 1444 с бонусным счётом, 1508 с кешбэком, 151 с днём
  рождения, 2998 с телефонами, 1232 с полом, у всех — дата заведения).
  Адреса, комментария и «места добавления» в ключе нет вовсе — их можно
  донести файлом через «Импорт клиентов».
- [x] 18.08 · Меню «⋮» на кнопке продажи: продавец и роль, «Отложить чек»,
  красное «Отменить чек», «Отмена» с Esc; три точки нажимаются только когда в
  чеке есть товары, пустая полоса продажи бледная.
- [x] 18.08 · Очередь чеков: отложенных сколько угодно, кнопки
  «ОЧЕРЕДЬ ЧЕКОВ: N» (оранжевая) и «НЕПРОВЕДЕННЫЕ: 0», окно со списком,
  «ВЫБРАТЬ» возвращает чек на кассу, «УДАЛИТЬ» выбрасывает.
- [x] 18.08 · «Настройки → Категории»: показ товаров из скрытых категорий,
  цвет, порядок перетаскиванием (проверено: строка переезжает, номера
  перенумеровываются), размер плитки, скрытие с витрины.
- [x] 18.08 · Меню «Действия N поз.» в справочнике: состав как у него;
  работают документы, цены и скидки, категории и группы, оценка склада,
  выгрузка, удаление в архив.
- [x] 18.08 · Касса открывается отдельным окном браузера; «Выйти» его
  закрывает; данные между окнами сверяются раз в две секунды. Проверено на том
  самом файле, который отправлен. **Важно:** внутри страницы показа по ссылке
  (она в рамке) браузер окна не даёт — касса открывается там же и говорит об
  этом строкой сверху. Отдельное окно работает, когда файл Wayshop открыт
  прямо в браузере.
- [x] 18.08 · Язык меняется во всей программе, а не только в кабинете;
  словарь сверен с их же файлами локализации (шесть языков).
- [x] 18.08 · Шрифт кассы — их: Fira Sans Extra Condensed, основной размер
  14 px; размеры экранов сведены к их шкале.
- [x] 18.08 · Настройки кассы перестали подтормаживать: своё состояние вместо
  общей ревизии базы, плитка витрины больше не перерисовывается впустую.
- [x] 17.08 · Ползунок витрины: от 5 плиток в строке до 7.
- [x] 17.08 · Всплывающая подсказка с полным названием товара.
- [x] 17.08 · Черта под «Меню» в нижней полосе.
- [x] 17.08 · Журнал чеков с отбором по дате, номеру, клиенту и товару;
  календарь и выбор года.
- [x] 17.08 · «Список должников» и возврат долга.
- [x] 17.08 · Продажа в минус — настройкой.
- [x] 19.08 · Движение денег по его снимку: счёт и номера. Способ оплаты я
  читал из `payment_terms`, где его нет вовсе, — и все 45 тысяч чеков выходили
  наличными, счёт всегда «Касса магазина». Оплата лежит в `orders` (это и есть
  «Приход #46047»), у каждого признак «наличные/терминал»: вышло 29 362 картой
  и 16 407 наличными. Номера теперь его: «Продажа #45868» и «Приход #46049»,
  а не наши внутренние «#1». Сверено с его снимком построчно — совпадает.
- [x] 19.08 · **Вся история покупок: 45 769 чеков с 2019 года.** Через ключ
  она достаётся окнами не длиннее 31 дня (`time_start`/`time_end`); без дат
  CloudShop отдаёт последние семь суток — на это я и напоролся сначала,
  решив, что глубже недели истории нет. Перенос идёт помесячно от свежего к
  старому. Проверено в карточке: «Рудник Михаил, создан 09.02.2022, 82 чека
  на 170 201,37» — покупки с 2022 года.
- [x] 19.08 · **Новый файл привозит новые данные.** Он открыл присланный
  файл и не увидел истории — и был прав: браузер держит базу от прежнего
  файла, отметки «уже загружено» стоят, и свежая выгрузка не заводится.
  Снаружи выглядит как несработавший перенос. Теперь у выгрузки есть
  подпись; не совпала — данные заводятся заново. На это есть проверки.
- [x] 19.08 · Дата добавления клиента: CloudShop отдаёт её меткой времени,
  без неё вся база выглядела заведённой в день переноса.
- [x] 19.08 · Выгрузка и загрузка справочников файлом. «Клиенты → Скачать в
  Excel» отдаёт всю карточку: телефоны (все), почту, день рождения, пол,
  адрес, комментарий, скидку, дисконтную карту, вид лояльности, бонусы,
  потраченные бонусы, кешбэк, кто и когда завёл, покупки и последний чек.
  В каталоге к «Остаткам и ценам» добавились «Все поля карточки» — с кодом,
  штрихкодом, PLU, описанием, весом и размерами.
  Главное: **файл возвращается обратно**. Заголовки колонок — те же слова,
  по которым импорт узнаёт поля, совпадение идёт по телефону, а пустая
  клетка не затирает заполненную. Заодно нашлась старая беда: обратная
  загрузка обновляла только четыре поля и молча обнуляла бонусы, кешбэк,
  день рождения и вторые телефоны. Теперь обновляется вся карточка,
  и на это есть проверки.
- [x] 19.08 · Поиск клиента по ФИО **и** по телефону: слова ищутся по
  отдельности, поэтому находится и «Рудник Михаил», и «Михаил Рудник», и
  «Рудник» вместе с четырьмя цифрами номера. Телефон — как его ни набери:
  «+7 (961) 253-27-57», «89612532757», «8 961 253 27 57», «2532757».
  Работает и в справочнике клиентов, и на кассе, и в отборе чеков.

- [x] 21.08 · **Бонусы клиентов — что есть в CloudShop и что перенесено.**
  Он сказал, что бонусы к клиентам не записаны. Проверил ключом, а не на
  словах:
  * счёт, потраченные бонусы и ставка кешбэка приходят карточкой клиента
    (`loyalty.bonus_balance` / `bonus_spent` / `cashback_rate`) и перенесены
    все — у 1 453 карточек счёт ненулевой. Видно в карточке: «На счету
    164,50 · Потрачено 50,00 · Возврат с покупки 3 %».
  * построчное «начислено / списано» в чеках CloudShop появляется **только
    с 2026 года**: до этого полей `bonus_cashback` / `bonus_spent` /
    `bonus_discount` в `legacy_line` нет вовсе — проверил окна за май 2022,
    ноябрь 2023 и март 2025, их там нет ни у одного чека. Отдельного
    запроса про бонусы у API нет: `/customers/{id}/bonus`, `/loyalty`,
    `/documents/bonus` и ещё шесть — 404.
  Поэтому у большинства клиентов счёт есть, а строк движения по нему нет.
  Дорисовывать недостающие по ставке кешбэка я не стал: это были бы не его
  данные, а мои выдумки. В карточке так и написано.
- [x] 21.08 · **Вся история — 45 811 чеков, и в CloudShop больше нет.**
  Между 8 марта 2024 и 31 января 2025 в кабинете нет ни одного документа:
  API отдаёт по этим месяцам пусто, а номера документов идут подряд через
  разрыв — 29968 и сразу 29969. То есть это не пропуск переноса, а
  перерыв в самом CloudShop.
- [x] 21.08 · **По ссылке теперь вся история, а не последние 18 тысяч
  чеков.** Страница не может быть больше 16 МБ, а история текстом — 14 МБ,
  и её приходилось обрезать. Теперь данные едут не внутри сборки, а рядом с
  ней, пожатые gzip: те же 45 811 чеков вместе с клиентами и фотографиями
  весят 3,3 МБ. Распаковывает браузер при первом запуске. Файл со своими
  данными похудел с 27 МБ до 10.
- [x] 21.08 · **Карточка клиента на телефоне — как на большом экране.**
  Раньше там была одна форма правки: имя, телефон, почта, скидка. Ни
  бонусов, ни истории покупок, ни статистики — и выходило, что бонусы «не
  перенеслись», хотя они были в базе, просто смотреть их было негде.
  Теперь на телефоне то же, что и в кабинете: ярлыки со счётом и кешбэком,
  контакты, статистика и три вкладки — покупки, деньги, бонусы. Правка —
  по кнопке.

- [x] 21.08 · **График выручки — как у него: линией, с заливкой и подписью
  под курсором.** Был столбиками. По его графику водят мышью и смотрят
  выручку конкретного дня — столбики так не умеют. Теперь сглаженная линия
  с заливкой, точки по дням, пунктир и кружок на выбранном дне, число рядом.
  Клик закрепляет день, второй клик снимает. Ось размечена круглыми числами:
  при вершине 46 501 — 0…60 000 с шагом 10 000, ровно как у него. На телефоне
  остаются столбики: `<svg>` там рисовать нечем.
- [x] 21.08 · **Почему выручка не сходилась: периоды считались не так.**
  У него за месяц 440 043,23, у нас выходило 609 819,86 — мы брали
  последние 30 суток, а кабинет считает **календарный** месяц. Видно и по
  его графику: ось 1…31, дни после сегодняшнего на нуле. Теперь месяц —
  с первого числа по последнее, квартал и год — тоже календарные, неделя —
  с понедельника. За тот же август у нас теперь 439 468,22 при 558 чеках
  и среднем чеке 787,58 — разница с кабинетом в 575,01 это две продажи,
  пробитые уже после выгрузки. День 15 августа на графике — 32 125, у него
  на снимке ровно столько же.
- [x] 21.08 · **День без продаж больше не сдвигает график.** Отчёт
  возвращает только те дни, в которые торговали, а график рисовал точки
  подряд — и вершина оказывалась не в своём дне. Теперь ряд раскладывается
  по номерам дней, пустой день — ноль. На это есть проверки.

- [x] 21.08 · **Себестоимость и цена закупки перенесены — всё, что отдаёт
  ключ.** В карточке товара (`/product`) их нет вовсе, и я сперва сказал,
  что взять их неоткуда. Нашлось: в строках документов «Корректировка»
  (`/documents/changes`) лежит `legacy_line.prices` — там и `cost`
  (себестоимость), и `purchase` (цена закупки), теми числами, какие стояли
  в карточке на день документа. Перенос читает 3 316 таких документов от
  свежих к старым, первое значение и есть нынешнее. Из 591 товара
  себестоимость нашлась у 107, цена закупки — у 271: у остальных её нет и
  в самом кабинете.
  Точной сходимости с его отчётом отсюда не будет, и я это не скрываю: у
  нас за август себестоимость продаж 41 278,00 против его 32 979,88.
  Кабинет берёт себестоимость **на момент чека**, а карточка хранит
  нынешнюю — это разные числа для старых продаж, и через ключ историю
  себестоимости не достать.

- [x] 21.08 · **Движение товара и движение денег — по его снимкам.** Он
  сказал, что присылал их и я не исправил. Присылал: снимки нашлись в
  переписке, сделал по ним.
  * **Строка отбора над таблицей.** У него это отдельные поля с серой
    подписью сверху и крестиком: в движении товара — дата, статус, оплата,
    тип; в деньгах — дата и тип. У нас всё пряталось за одной кнопкой
    «Фильтр», и чтобы отобрать чеки за неделю, надо было открыть ящик,
    полистать и закрыть. Кнопка осталась — за ней то, чему в строке места
    нет: отправитель, получатель, автор, счёт.
  * **Колонка «Статус»** подписана, а не пустая.
  * **Колонка «%»** сразу за суммой — у чеков, как у него.
  * **Значок комментария** у номера документа, если в нём есть заметка.
  * **Автор — настоящий.** В обеих таблицах в колонке «Автор» стояло слово
    «waystea», написанное прямо в разметке: у чеков «Чайного бара» и
    «Черёмушек» тоже значился владелец. Теперь там тот, кто пробил чек.
  * **Ноль — это ноль.** Чек на 0.00 показывался прочерком, будто сумма
    неизвестна. Прочерк остался только у складских документов, где оплаты
    нет вовсе.
  Поля «статус» в движении денег у нас нет, хотя у него оно есть: денежный
  документ всегда проведён, отложенных не бывает, и это поле не отбирало бы
  ничего. Рисовать кнопку, которая ничего не делает, я не стал.

- [x] 21.08 · **Документ открывается панелью поверх журнала — как у него.**
  Он кликнул «Продажа #45904» в «Движении товара» и прислал, что должно
  получиться. Было: уход на отдельную страницу, журнал пропадал вместе с
  отбором и местом прокрутки. Стало: панель справа, список остался слева,
  закрыл — и ты там же.
  По его снимку добавлено: полоса действий сверху (✕, «Редактировать»,
  печать, выгрузка, возврат, «Удалить» справа), значок календаря у «Создан»,
  стрелка у «Документ проведён», третья фишка «Статус заказа», синяя точка
  и номер ссылкой в таблице оплаты, поиск по товарам документа, колонка
  **«Скидка»** в таблице товаров и «-» под ценой в строке «Итог».
- [x] 21.08 · **Скидка в строке чека.** У него в «Продаже #45904» первая
  позиция продана за 170.10 со скидкой 170.10 и итогом 0.00, а остальные —
  без скидки. Мы хранили одну скидку на весь чек, и такое было не выразить.
  В перенесённой истории скидка по строкам была всё это время — приезжала
  из CloudShop и молча отбрасывалась. Теперь она в базе (миграция 25) и в
  документе.
  Не сделано: **«Касса» и «Смена»** у перенесённых чеков остаются пустыми.
  Ключ отдаёт только их внутренние номера (`references.register_id`,
  `shift_id`), а названия кассы и номера смены — нет: запросов `/registers`
  и `/shifts` в Connect API нет вовсе, а `/company` мой ключ не пускает
  (403). Придумывать «#3421» я не стал. Если добавить ключу право на
  «Компанию», попробую ещё раз.

- [x] 23.08 · **Начисление и списание бонусов на кассе.** Счета
  перенеслись, карточка их показывает, история — тоже, а торговать по ним
  было нельзя: касса про бонусы не знала вовсе, и покупатель, которому
  обещали кешбэк, после покупки видел прежний счёт.
  Теперь в окне оплаты у бонусного покупателя стоит строка «Списать бонусы
  (до …)»: нажал — сумма чека уменьшилась, деньгами принимается остаток.
  Предел берётся самый строгий из трёх — счёт покупателя, лимит компании и
  сам чек. Кешбэк начисляется с того, что заплатили **деньгами**: за часть,
  закрытую бонусами, бонусы не идут, иначе счёт рос бы сам из себя. Своя
  ставка покупателя (`cashback_rate` из CloudShop) важнее общей.
  Проверено в собранной кассе: чек 60,00, покупатель Монтеро Антонио со
  счётом 164,50 → «Списать бонусы (до 30,00)» → «Бонусами − 30,00»,
  «К оплате 30,00». После продажи в карточке «Бонусы: 135.40» и
  «Потрачено бонусов: 80.00» — 164,50 − 30,00 + 3 % с тридцати.
  Заодно исправлена подпись под «Курсом списания»: она обещала «1 бонус =
  100.00 скидки», то есть ошибалась в сто раз. Заметить это можно было,
  только начав списывать бонусы, — до сих пор было нечем.

- [x] 23.08 · **Опись всего, что отдаёт CloudShop, и перенос всего разом.**
  Он сказал, что устал показывать пальцем, где что взять. Справедливо.
  Взял у CloudShop список **всех** его запросов (`connect-api.cloudshop.ru/docs`
  — открытая опись, 18 штук) и прошёл по каждому. Что нашлось сверх
  прежнего переноса:
  * **689 снятых с продажи товаров** (`/product/deleted`) — их больше, чем
    живых. Заводятся архивными: в справочнике и на витрине не видно,
    продать нельзя, история цела.
  * **270 наборов** («В зале …» и прочие позиции с составом): в общих
    списках их нет вовсе, но `/product/{id}` отдаёт каждую поштучно.
    Строки истории теперь находят товар в **99,3 %** случаев вместо 85,7 %:
    осталось 761 строка из 104 723, и у них в самом чеке нет кода.
  * **9 возвратов продаж** (`/documents/return?type=sales`) — своего раздела
    в переносе не было вовсе. В журнале стоят рядом с чеками и называются
    «Возврат продажи»; на это есть проверка.
  * **Себестоимость и цена закупки** — из документов корректировки
    (сделано накануне).
  Ничего не отдаёт ключ по трём вещам, и это не моя лень:
  * `/company` и `/company/statistic` — **403**, ключу не хватает прав на
    раздел «Компания». Если добавить их в кабинете, заберу и это.
  * Названия касс и номера смен: запросов `/registers` и `/shifts` в
    Connect API нет вовсе, в документе лежат только внутренние номера.
  * Групп товаров нет: есть категории (одна, «Зал») и 12 меток —
    «Улунский чай», «Шу Пуэр» и прочие, они и стоят у нас категориями.
  Итог переноса: **1550 карточек товаров** (591 живая + 959 архивных),
  **3221 клиент**, **45 864 документа** истории.

- [x] 23.08 · **Окно «Фильтр» в каталоге — по его снимку от 14 августа.**
  Снимок он прислал 14-го, а у меня в списке до сегодня стояло «жду
  снимки»: я просил то, что уже лежало в переписке. Виноват.
  Было: восемь неподвижных галочек — «нет в наличии», «нулевая
  себестоимость» и так далее. Они отвечали на восемь вопросов, а вопрос у
  него каждый раз свой.
  Стало ровно его окно и в его порядке: **Пресеты фильтров** ▾,
  полоса **Товар · Услуга · Комплект**, **Категории** ▾, **Цена**
  [базовая ▾][больше ▾][___], **Остатки** [общие ▾][больше ▾][___],
  **Срок годности** «истекает в течение [___] дней», **Изменения товара**
  «изменялся в течение [___] дней», **Продаваемость** «продавался в течение
  [___] дней», и три кнопки — зелёная «Применить», «Сбросить», «Сохранить
  пресет» зелёным контуром.
  Части складываются по «и». Проверено в собранной программе: «базовая цена
  больше 500» — 591 позиция превратилась в 263, кнопка стала «Фильтр: 1».
  На это есть девять проверок в базе.
  Заодно завелась дата правки карточки (миграция 27): полю «изменялся в
  течение N дней» отвечать было нечем — она нигде не хранилась.
  **Не сделал одного:** у него рядом с «Ценой» и «Остатками» есть кнопка
  «+» — добавить второе условие того же вида («цена больше 500 и меньше
  900»). Пока одно условие на строку; скажете — добавлю вторую.
- [x] 23.08 · Меню «Действия» в каталоге сверил со снимком заново: порядок и
  пункты совпадают до строки — «Создать документ», «РАБОТА С ГРУППОЙ
  ТОВАРОВ» (Цены и скидки, Категории и группы, Другое), «Ценники»,
  «Редактор цен», «Оценка склада», «Файл для весов», «Скачать в Excel»,
  «Удалить». Приглушены те, что ещё не сделаны.

- [x] 23.08 · **Всё синее — нажимается.** Он сказал: «везде можно
  посмотреть на что угодно кликнув». У нас поля были синими, но никуда не
  вели — цвет обещал переход, которого нет. Куда именно ведёт каждое поле,
  я взял не на глаз: `web.cloudshop.ru` отдаёт разметку своих экранов без
  входа, и в `js/pages/journal/page/index.html` прямо написано —
  `card.profile` у автора, `card.register.showBox` у кассы,
  `card.register.showShift` у смены, `card.money_show` у номера оплаты,
  `card.account_show` у счёта, `card.catalog.get` у товара.
  Теперь ведут: в журнале — отправитель, получатель, автор; в документе —
  магазин, клиент, автор, касса, смена, номер прихода, счёт, контрагент и
  каждый товар.
- [x] 23.08 · **Касса и смена у перенесённых чеков.** Я говорил, что взять
  их неоткуда. Был неправ наполовину.
  *Касса* — есть: запроса `/registers` в API нет, но касса и её «магазин»
  заводятся парой и различаются последним знаком идентификатора. Касса
  `…7596` — это магазин `…7597` с названием «№1». Сходится с его
  документом: там, где он видит «Касса: №1», у нас теперь тоже «№1».
  Проверено на всех трёх: «№1», «Черёмушки», «Чайный бар».
  *Смена* — номер наш. Ключ отдаёт только внутренний номер, поэтому смены
  пронумерованы по времени первого чека: их вышло **3420**, а у него в
  документе от 23 августа стоит **#3430**. Разница в одиннадцать — это
  смены, которые открыли и закрыли без единой продажи: в выгрузке чеков их
  не видно вовсе. Подгонять на одиннадцать я не стал.
- [x] 23.08 · **«Редактировать» работает.** Он спросил: «а почему нельзя
  редактировать». Потому что я нарисовал кнопку и приглушил её. Теперь
  правка настоящая: в документе меняются количество и цена строки, строку
  можно убрать, «Сохранить» переписывает строки и **движения склада** одной
  транзакцией — остаток сходится с новым количеством сам. Номер чека,
  время, смена, касса и бонусы правкой не трогаются: по номеру чек ищут, а
  бонусы уже записаны покупателю на счёт.
  Проверено в собранной программе: в «Продаже #45967» количество 1 → 5,
  строка стала 1 250,00, итог чека пересчитался. На это шесть проверок.
  Заодно заработали «Напечатать» (окно печати браузера) и «Выгрузить»
  (строки документа файлом).
- [x] 23.08 · **Журнал открывается неделей**, как у него: в поле «дата»
  стоит «17 авг — 23 авг». Сорок пять тысяч документов одним списком —
  это стена, а не журнал.
- [x] 23.08 · **Навык «ничего не приглушать».** Он потребовал завести
  правило, чтобы это не повторялось. Завёл:
  `.claude/skills/ничего-не-приглушать/SKILL.md` — мёртвых кнопок и синего
  текста, который никуда не ведёт, в программе не бывает; куда что ведёт —
  смотреть в их разметке; снимок сперва искать в переписке, а не просить
  заново; прочерк ставить только исчерпав способы достать значение.

- [x] 23.08 · **Правка документа — отдельной страницей, как у него.** Он
  прислал снимок: `card/doc/show/<id>`, заголовок «Документы /
  редактирование документа» — это **другой экран**, а не просмотр с полями
  вместо текста. Я сперва сделал правку внутри просмотра; переделал.
  Разметку взял у них же (`js/pages/card/documents/_view.html` и
  `form/products.html` — отдаются без входа), оттуда и порядок колонок.
  Теперь страница его: зелёная «Сохранить» и «Печать ▾» слева, «Удалить»
  справа; переключатель «Документ проведён», галочка «Заказ», дата с
  карандашом; номер документа правится прямо в заголовке; поля «Магазин» и
  «Клиент»; вкладки «Товары» и «Счета и оплата»; поиск по товарам документа
  и кнопка «Цена продажи»; таблица из двенадцати колонок — №, наименование,
  код товара, артикул, штрих-код, налог, ед. изм., кол-во, цена, скидка **в
  процентах**, итог и крестик; ниже «Итог / Оплаченные / Налоги» и подпись
  «кто и когда завёл».
  Правятся жёлтые клетки — количество, цена, скидка. Сохранение переписывает
  строки и движения склада одной транзакцией.
  Проверено рядом с его снимком: «Продажа # 45967», строка 2 — 3 × 10,5,
  скидка 100 %, итог 0.00; итог документа 250.00 руб. Совпадает.

- [x] 24.08 · **Движение денег — как в кабинете.** «Сделай так же движение
  денег аналогично как в клоуд шопе». Смотрел не на глаз, а в их разметку:
  `js/components/money-table/_view.html` — и оказалось, что на страницу
  документа (`card.money_show`) у них ведёт **каждая** строка, а не только
  заведённая руками. Приход по чеку у них тоже документ, просто с
  «Привязать к документу» вместо правки. Сделал так же:
  - строка ленты открывает `\`/money/show/<id>\`` — и своя, и из чека;
  - страница собрана по их `js/pages/orders/page/_view.html`: «Документ
    проведён», дата с карандашом, «Приход # 46148», строка «Касса №1 ·
    Смена #3419», поля «Счёт» и «Контрагент» (у перевода — «Со счёта» и
    «На счёт»), «Категория платежа», «Сумма, руб», «Комментарий», «Итог»;
  - свой документ здесь же и правится: счёт, контрагент, категория, сумма,
    комментарий и дата (карандаш), кнопки «Сохранить» и «Удалить»;
  - у прихода по чеку правки нет — вместо неё «Привязать к документу»
    с переходом на чек и зелёная «Открыть чек»: сумма там держится
    проданным товаром, и менять её надо в чеке;
  - синие столбцы ленты ведут туда же, куда у него: контрагент — в его
    карточку, счёт — в «Счета», автор — в «Сотрудники»;
  - названия страниц взял из их же словаря (`languages/ru.json`):
    «Просмотр ордера» и «Просмотр документа».
  Переключатель «Документ проведён» заменил галочкой: отложенных денежных
  документов у нас не бывает, а выключатель, который нельзя выключить, —
  это ровно та мёртвая кнопка, за которую он ругался.
  Проверено в собранной программе: приход #46148 — Терминал / Счет в банке,
  250.00, «Продажа #45967»; свой расход #1 после правки даты и суммы нашёлся
  в ленте 1 августа со значением 2 000,00.

- [x] 24.08 · **Фотографии крупнее.** «У товаров очень плохого качества
  фото» — и правда: при переносе они ужимались до 96 точек, а карточка
  показывает снимок квадратом 150×150 и растягивает его вдвое на хорошем
  экране. Переснял все 967 штук по 288 точек — 21 КБ вместо 2,6 КБ.
  Ключ для этого не нужен: их хранилище отдаёт файлы всем, адреса лежат в
  выгрузке. Отдельная команда, чтобы не гонять весь перенос:
  `node scripts/refresh-photos.mjs`. Файл вырос с 10,9 до 26,1 МБ и
  открывается за 18 секунд.

- [x] 24.08 · **Четыре приглушённых пункта в меню «Действия» заработали.**
  - **Ценники** — открывают конструктор ценников (он уже был сделан, просто
    из меню в него не пускали).
  - **Редактор цен** — свой экран по их разметке
    (`card/catalog/price-editor`): зелёная «Сохранить», поиск, таблица
    «Название · Артикул · Штрих-код · Себестоимость · Закупочная цена ·
    Цена продажи» и колонка на каждый магазин. Правится полсотни строк
    подряд и сохраняется одним нажатием.
  - **Другое** — срок годности, НДС и признаки (весовой, подакцизный,
    свободная цена) сразу списком. Пустое поле значит «не трогать».
  - **Файл для весов** — больше не «нужна модель весов». Кабинет её и не
    спрашивает: даёт выбрать из трёх (МАССА-К, MERTECH M-ER, RONGTA) и
    отдаёт таблицу с колонками этой модели. Наборы колонок взяты из их же
    собранной страницы, колонка в колонку. Проверено: файл скачивается,
    заголовок и цены на месте.

- [x] 24.08 · **Цена дороже тысячи не сохранялась.** Нашлось, пока писал
  проверки на весы. Экран пишет суммы по-английски — «1 300.00» с запятой
  в тысячах, — и эта же строка стоит в поле ввода. Разбор читал запятую как
  дробную часть, получалось «1.300.00», и число не читалось вовсе: открыть
  карточку товара за 1 300 ₽ и нажать «Сохранить», ничего не меняя, значило
  потерять цену. На товарах дешевле тысячи это не проявлялось, потому и
  жило долго. То же было с количествами. Теперь разбор общий и понимает оба
  вида записи. Проверено в собранной программе: 1 300,00 → 1 450,00,
  сохранилось и пережило перезагрузку.

- [x] 24.08 · **«В карточке товара качество картинки тоже очень плохое».**
  Две причины, обе убраны. Первая: снимок **растягивался**, а не вписывался
  — у картинок стоял `object-fit: fill`, и квадратное фото размазывалось по
  прямоугольной ячейке. Теперь вписывается, во всех пяти местах: карточка
  товара, справочник, редактор цен, плитка кассы. Вторая: по ссылке лежали
  снимки, ужатые до 96 точек. Теперь у действующих товаров — 208 точек,
  вдвое крупнее, чем показывает карточка. У архивных оставлены мелкие: их
  492 из 942, и показываются они только значком 40×40 в списке. Без этого
  страница не влезала: у ссылки предел 16 МБ, а с крупными снимками у всех
  подряд выходило 26. В файле, который открывается с диска, предела нет —
  туда идут снимки по 288 точек.

- [x] 24.08 · **«Проверь шрифты, масштаб, последовательность колонок в
  движении товара и сделай 1 в 1».** Порядок колонок совпадал и до
  проверки: Статус · Документ · Время · Позиций · Сумма · Оплаченные ·
  Отправитель · Получатель · Автор. А оформление врало, и числа взяты не на
  глаз, а из их таблицы стилей `css/my-*.css`:

  | | было | стало (их правило) |
  |---|---|---|
  | Заголовки | 14 пт полужирные, «Документ» | 11 пт обычные ЗАГЛАВНЫМИ — `font-weight:400; font-size:.8em; text-transform:uppercase` |
  | Ширины | свои, шире; «Автор» уезжал за край | 60 / 180 / 80 / 80 / 110 / 110 / 180 / 180 / 180 |
  | Значок «%» | отдельной колонкой | внутри ячейки суммы |
  | Полоска слева | лента на всю группу | `top:4 bottom:4 left:4 width:4 radius:2` — метка у строки |
  | Галочка | бирюзовая, непроведённому карандаш | галочка всегда, `#4183C4` / `#bbb` |
  | Высота строки | 46 | 38 (`padding: .78571429em`) |

  Шрифт проверен отдельно, через `document.fonts`: грузится и применяется
  Roboto 400/500/700 — тот самый, что в их `body{font-family:Roboto,…}`.
  Подмены на системный нет.

  Не сходится честно: значка 💬 у чеков #4917 и #4916 нет — комментарии к
  чекам выгрузка не отдаёт.

- [x] 24.08 · **«А где всё движение товаров? Куда делось?»** Ничего не
  пропало — журнал прятал всё сам. Он открывался календарной неделей «в
  которой мы сейчас», а перенесённая история кончается двадцатым августа:
  на дворе двадцать четвёртое, отбор стоял «24 авг — 30 авг», и список
  выходил пустым. В его кабинете этого не видно только потому, что там
  торгуют каждый день. Теперь неделя отсчитывается от последнего дня, за
  который в базе есть документы (`lastJournalDay`). Проверено: журнал
  открывается сам, 274 документа за 18–24 августа.

- [x] 24.08 · **«Шрифт как был крупный, так и остался».** Был прав: журнал
  у меня был набран кеглем справочника. У них это две разные таблицы, и
  правило нашлось в их же `css/my-*.css`: справочник лежит в
  `.table-content`, где стоит `.ui.table{font-size:14px}`, а журналы
  движения товара и денег — в `.fixed-title`, где такого правила нет и
  работает `.ui.small.table{font-size:.9em}`, то есть 12,6. Заголовок
  колонки — 0,8 от неё, десять. Развёл: в журналах строка 13 и заголовок
  10, в справочнике остались 14 и 15 (название товара у них подписано
  отдельно, `font-size:15px`). Сверено по ширине одной строки: «Продажа
  #45967» занимает 104 точки против его 105.

  Заодно движению денег даны их ширины колонок — 180 / 80 / 110 / 110 /
  180 / 180 / 180 / 180 из `.table.order-table tr td[data-name=…]`: там
  «Автор» тоже уезжал за край.

- [x] 24.08 · **«То же самое по логике сделай и с движением денег».**
  Сделано, и по тем же их правилам:

  - **Неделя по последнему дню с деньгами.** Отбор считается от последнего
    дня, за который в базе есть движение денег, а не от сегодняшнего. День
    у денег свой: приход рождается чеком, расход — заведённым документом,
    и последний день у них может не совпадать (`lastMoneyDay`). Проверено:
    журнал открывается сам, 166 документов за 17–23 августа.
  - **Ширины колонок** — из `.table.order-table tr td[data-name=…]`:
    60 / 180 / 80 / 110 / 110 / 180 / 180 / 180 / 180. Порядок совпал с их
    разметкой и до правки: Статус · Заказ · Время · Приход, руб · Расход,
    руб · Контрагент · Счёт · Категория платежа · Автор.
  - **Кегль** — 13 в строке и 10 в заголовке, как в движении товара.
  - **Полоска слева** стала их `i.indicator`: `top:4 bottom:4 left:4
    width:4 radius:2`, зелёная у прихода и красная у расхода
    (`ng-class="{red: credit, green: debit}"`).
  - **Галочка синяя у всех.** Я красил её в цвет полоски, и красная
    галочка у расхода читалась как «с документом что-то не так». У них
    цвет галочки не зависит от типа: `color: '#4183C4'`.

  Расчёт недели вынесен из экрана в `src/domain/calendar.ts`
  (`weekEndingAt`) и закрыт проверками: ошибка тут в одни сутки и глазами
  не видна — неделя как неделя, просто не та.

- [x] 24.08 · **«Смотри, как открывается продажа в движение денег».** В
  присланном адресе — `web.cloudshop.ru/card/money/m/money/show/…`, и кусок
  `/m/` это их «панель»: ордер открывается **поверх списка**, а список
  остаётся слева. Я открывал его отдельной страницей, и список пропадал
  вместе с отбором и местом прокрутки. Теперь панель, размером как у него —
  около 840 точек.

  По его же снимку выправлено и содержимое:

  - заголовок **«Просмотр ордера # 46148»**, а не «Приход #»;
  - строки **Касса** и **Смена** подписью слева и синим значением;
  - подписи полей полужирные, со **звёздочкой** у обязательных;
  - **«Сумма, руб»** значением вправо;
  - **«Итога» внизу нет** — я его выдумал, у них его не бывает;
  - **«Счёт» не правится** у прихода по чеку — ровно как у него
    (`disabled="id && account.type == 'register'"`).

  И главное: приход по чеку у них **правится**, а я закрыл его целиком.
  Теперь правятся те поля, что и вправду лежат в чеке, — контрагент и
  комментарий (`updateSalePayment`). Сумму не даю: приход по чеку отдельной
  записью не лежит, он и есть итог чека, и её правка развела бы деньги с
  проданным товаром.

- [x] 24.08 · **«Кликаю на продажу, дальше всё внутри должно быть
  кликабельно: на контрагента кликаешь — его карточка должна открыться».**
  Он прислал снимок: адрес `card/journal/m/clients/show/…` — карточка
  клиента ложится **панелью поверх чека**, со стрелкой «назад» слева.

  Было хуже, чем «не открывается»: синее имя уводило на **список**
  контрагентов, а чек при этом закрывался. Теперь из чека открываются
  панелью, поверх и с возвратом назад:

  - **Клиент** → карточка контрагента;
  - **товар в строке** → карточка товара;
  - **номер оплаты** в блоке «Оплата» → сам ордер
    (`card.money_show({orderId})`), а не общий список денег;
  - **Контрагент** в строке оплаты → та же карточка.

  Заодно то же самое в лентах: имя в столбце «Получатель» движения товара и
  в столбце «Контрагент» движения денег открывает карточку, а не список с
  подставленным поиском (`findCounterpartyByName`). В движении денег имя и
  вовсе уводило на телефонный экран — этого на большом экране быть не
  должно.

  Подписи разделов карточки — заглавными, как у него: «КОНТАКТЫ»,
  «СТАТИСТИКА», «ПОСЛЕДНИЕ ОПЕРАЦИИ».

- [x] 24.08 · **«Проверь, всё так же, как на CloudShop?»** Проверил не
  глазами, а нажатием: написал обход, который находит на экране **всё синее
  с текстом** — нажимаемое и нет, — жмёт каждое и смотрит, изменилось ли
  что-нибудь. Молчащее синее и есть «мёртвая кнопка».

  | Экран | синих | работают | молчали |
  |---|---|---|---|
  | Движение товара | 37 | 37 | — |
  | Движение денег | 31 | 31 | — |
  | Товары и услуги | 23 | 23 | — |
  | Главная | 7 | 7 | — |
  | Клиенты | 38 | 38 | — |
  | Смены | 2 | 2 | — |
  | Внутри чека | 9 | 9 | — |

  Обход нашёл три настоящие поломки, все исправлены:

  1. **Последние строки списка уходили под подвал.** У таблицы стояла
     высота по содержимому (`maxHeight: 10000`), и нижние товары были видны
     краем, но не нажимались — сверху лежал подвал «Создать новый товар». У
     них такого нет: высота таблицы задана прямо
     (`.fixed-title div.table { height: calc(100vh - 129px - 46px) }`).
     Теперь таблица кончается над подвалом; то же в обеих лентах.
  2. **Синий «⊞» рядом с «Создать новый товар» молчал** — нажималась только
     подпись. Стал одной кнопкой со значком.
  3. **В «Оценке склада» ссылки никуда не вели**: «417 поз. с
     себестоимостью равной 0 руб» и «151 поз. с остатком меньше 0». Теперь
     открывают справочник, отобранный именно по этому условию (в отбор
     добавлена себестоимость — `price.field: 'cost'`).

  Плюс числа под графиком на главной: они были синими и молчали. Теперь
  нажимаются — выбирают день, как нажатие по самой линии. Синим отмечена
  каждая седьмая метка: это его разметка недель, а не ссылки.

- [x] 24.08 · **Клиенты: шрифты, шапка, последовательность.** Сверил не с
  памятью, а с их `columnSettings` в `main-*.js`. Расхождения были:

  - в «Информации» у меня стояли **Скидка** и **Бонусов** — их я вставил
    сам «чтобы главное было видно сразу». У него они живут в «Лояльности»,
    и от этой самодеятельности сбивался весь порядок. Убрал;
  - «Статистика» **обрывалась на расходах**: не было «Долг по продажам»,
    «Долг по возвратам», «Баланс». Добавил;
  - первый столбец назывался **«Наименование»**, у него — **«Клиент»**;
  - пустые столбцы у меня **пропадали** («Описание», «Адрес», «Добавил»).
    Он попросил сверить верхнюю строку и её последовательность — значит,
    столбец стоит на месте, даже когда его ещё не заполнили;
  - кнопка **«Настройка системы лояльности» была приглушена**. Теперь
    открывает раздел лояльности.

  «Лояльность» совпала с его набором и до правки.

  Шрифты сошлись: заголовок 10 пт заглавными, ячейка 14, имя клиента 15 —
  ровно как в их `.table-content .ui.table`.

- [x] 24.08 · **Качество фотографий и снимок в чеке кассы.** Проверил
  запасом: во сколько раз исходник крупнее того, как его показывают.

  | Где | Исходник | Показан | Запас |
  |---|---|---|---|
  | Список товаров | 288 | 40 | 7,2× |
  | Карточка товара | 288 | 148 | 1,95× |
  | Плитка кассы | 288 | 148 | 1,95× |
  | Строка чека | 288 | 44 | 6,5× |

  Нашлась **моя оплошность**: последние сборки я делал с ключом
  `--photos=photos-mixed.json` — это набор **для ссылки**, где у товаров
  208 точек вместо 288. Файл ему уходил с мелкими снимками. Порядок сборки
  обеих версий записан в `docs/сборка.md`, чтобы не повторилось.

  И сделано то, чего не было вовсе: **снимок товара в чеке справа**.
  Кассир выбирает товар по картинке в плитке, а в чеке видел только
  название — «Чай с собой большой стакан» и «средний» различаются одним
  словом.

- [x] 24.08 · **«Во вкладке клиенты шрифты и последовательность».** Пошёл
  проверять и нашёл то, чего не видел раньше: **шапка подчёркнута, но не
  сортирует**. У него подчёркивание значит именно сортировку —
  `sortable-table-link` в `js/pages/clients/_list.html`, и в
  `columnSettings` у каждой колонки стоит `sorting: true|false`. У меня
  подчёркивание было украшением, то есть обманом.

  Сделана настоящая сортировка — в клиентах и в справочнике товаров:

  - нажатие по названию сортирует, второе нажатие переворачивает; рядом
    стрелка ▲▼;
  - подчёркнуты **только** сортируемые: телефон и почта у него не
    сортируются (`sorting: false`), и подчёркивать их нельзя;
  - открывается по имени и по возрастанию — его `{field: 'name', reverse:
    false}`;
  - число сортируется числом: «1 000,00» и «900,00» строками сравнивались
    бы наоборот;
  - день рождения — по годам: в выгрузке он лежит как «13/07/2006», и
    строкой сортировался бы по числу месяца;
  - пустое всегда внизу, в обе стороны;
  - буквы — по русским правилам (`localeCompare('ru')`): у «ё» код больше,
    чем у «я», и «Ёлчиев» вставал после «Ясонова».

  Про шрифты и последовательность: в собранном файле шапка идёт **КЛИЕНТ ·
  ТЕЛЕФОН · EMAIL · ДЕНЬ РОЖДЕНИЯ · ПОЛ · ОПИСАНИЕ · АДРЕС · ДОБАВИЛ ·
  СОЗДАН** — это его `columnSettings.basic`. Кегли: заголовок 10 заглавными,
  ячейка 14, имя клиента 15 — как в их `.table-content .ui.table`.

- [x] 24.08 · **«Размер шрифта так и не изменился, толщина столбцов
  тоже».** Он был прав дважды, и ошибка была моя: я приписал справочникам
  не тот контейнер.

  В их вёрстке **все** большие таблицы — журналы, товары и клиенты — лежат
  в `<div class="content full-width fixed-title">` и набраны
  `ui small celled table scroll main-table`. Правила `font-size` для
  `.fixed-title` нет, поэтому работает `.ui.small.table{font-size:.9em}` —
  **12,6** от четырнадцати. А правило `.table-content .ui.table
  {font-size:14px}`, на которое я опирался, относится к **другому**
  контейнеру. Я оставил справочникам 14 и 15, и он дважды сказал, что
  шрифт крупный.

  Теперь в клиентах и в товарах строка 13, как в журналах.

  Ширины: у него `.fixed-title .ui.table tr td { min-width: 75px }` и
  жёстко заданы только особые виды столбцов — цена 100, штрихкод, артикул
  и страна 140. То есть все столбцы одной ширины, кроме имени. У меня они
  были подобраны на глаз, от 110 до 320, и таблица выходила заметно шире
  его — это он и назвал «толщина столбов». Стало: имя 260, остальные по
  140. Проверено в собранном файле: кегль 13, ширина каждого столбца 140.

- [x] 24.08 · **Снимок вкладки «Клиенты» и главная.** По его снимку
  `card/clients` выправлено то, что глазами я не поймал:

  - **вертикальные линии между столбцами** — у них таблица `celled`, и
    `.fixed-title .ui.table thead th` несёт `border-right: 1px solid
    #f2f2f2`. Без них девять столбцов читались как каша;
  - **ширины сняты с его снимка**: имя 274, телефон 110, почта 151, день
    рождения 144, пол 78, описание 200, адрес 77, добавил 151, создан 95.
    У них ширина по содержимому с полом в 75, и пол с адресом стоят у
    самого пола — они почти всегда пустые;
  - **дата через косую черту**: «06/02/2023», а не «06.02.2023»;
  - **прочерк только у дня рождения** и обычным дефисом; у пола, описания
    и адреса пустая ячейка остаётся пустой;
  - **листалка со стрелками** «« ‹ … › »»: на шестидесяти пяти страницах
    без них до конца не добраться.

  **День рождения сдвигался на сутки.** У него «13/07/2006», у меня было
  «12.07.2006». CloudShop отдаёт дату меткой времени на местную полночь, а
  я брал из метки день по Гринвичу — в МСК это предыдущие сутки. В
  `scripts/import-cloudshop.mjs` метка теперь сдвигается на московский час.
  Уже перенесённые записи этим не лечатся: их надо перевыгрузить ключом.

  **Главная.** Кегли сверены с их `my-*.css`: ось выручки 12 пт, цвет
  `#B0B0B0`, начертание 300; ось дней 10 пт — у меня стояло 12 и 11 обычным
  начертанием, и ось выходила темнее и тяжелее. Заголовок 28 пт
  начертанием 300 цветом `#33425B` совпал и до правки.

  И вернулись **кружки «?»**: у него они стоят у «Выручки»,
  «Себестоимости продаж», «Прибыли», «Среднего чека» и у столбцов «Кол-во»,
  «Сумма», «Склад». У меня их не было вовсе, и «Прибыль» приходилось
  угадывать.

  Чего не сделал: столбец **«Добавил» пустой**. Кто завёл карточку,
  Connect API не отдаёт — в кабинете это видно, потому что там своя
  внутренняя выгрузка. Писать туда «waystea» на все три тысячи строк я не
  стал.

- [x] 25.08 · **«Движение товара»: оформление, графы, шрифты и комментарии
  к продажам.** Сверено с их шаблоном журнала
  (`js/pages/journal/page/index.html`, `js/components/doc-table/_view.html`)
  и таблицей стилей, а потом измерено в собранном файле, а не на глаз.

  Что сошлось и так: порядок граф (статус, документ, время, позиций, сумма,
  оплаченные, отправитель, получатель, автор), их ширины, кегль шапки 10
  прописными обычным начертанием, кегль строки 13, все числа по левому
  краю. Мне казалось, что сумма выровнена вправо, — померил: у неё
  `text-align: start`, как и у него. Показалось.

  **Комментарии к продажам — их не было вовсе.** У чека в CloudShop есть
  своё поле `comment`, я его никогда не забирал: ни в переносе, ни в
  показе. Теперь:

  - перенос забирает `comment` у продажи и `note` у складского документа;
  - в журнале рядом с названием документа стоит их значок комментария
    (серый, `<i class="comment icon">`), а не смайлик, как было у меня;
  - в самом чеке комментарий стоит кремовой плашкой между шапкой и
    «Оплатой» — их `.doc-comment`: фон `#FFFAF3`, кегль 12, скругление 6;
  - в движении денег у прихода по чеку тот же значок: у них он там тоже
    есть.

  Заодно: **имя покупателя больше не лежит в комментарии.** Я складывал его
  туда у чеков без карточки, и значок комментария загорался там, где
  комментария не было, а в поле «Комментарий» стояло имя человека. Теперь
  у чека своё поле `customer_name` (миграция 29).

  И **поиск по номеру** в обоих журналах: он сравнивался с внутренним `id`,
  а в строке стоит номер CloudShop. «45962» не находило «Продажа #45962».
  Теперь ищет по тому номеру, который написан.

  Мелочи по их шаблону: штрих-код и артикул в таблице товаров документа —
  по правому краю (у них вся таблица `right aligned`, слева только
  наименование); подписи полей шапки шириной 120 цветом `rgba(51,66,91,.7)`.

  Чего **не** сделал: у уже перенесённых чеков комментариев нет — они
  лежат в кабинете, и вытащить их можно только новой выгрузкой ключом
  Connect API. Скажете — перевыгружу, ключ возьму из переменной окружения и
  никуда не запишу.

- [x] 25.08 · **Строка отбора над обоими журналами: прокликать всё и
  сделать как у него.** Разобрал их `inline-filter`
  (`js/components/inline-filter/_view.html` плюс настройки полей в
  `journalCtrl` и `moneyCtrl`) — и оказалось, что устроена она у него
  совсем не так, как я сделал:

  - в строке стоят только заведённые поля, а кнопка **«Фильтр» — это
    список оставшихся**; выбрал из него поле — оно встало в строку и сразу
    открылось. У меня за «Фильтром» открывался ящик со всеми полями сразу,
    и счётчик «Фильтр: 2» — тоже моя выдумка, у него его нет;
  - **крестик убирает поле из строки**, а не просто чистит значение, и поле
    возвращается в список «Фильтра»;
  - у поиска крестика нет вовсе — он `required`
    (`.inline-filter-item.required a.label{display:none!important}`);
  - размеры и цвета сняты с их CSS: рамка `#f2f2f2`, скругление 3, высота
    поля 57, подпись на `#fafafa` кеглем 12 цветом `#666` строчными,
    значение кеглем 12, крестик 12 цветом `#999`, кнопка «Фильтр» высотой
    48 цветом `#666`.

  **Что внутри каждого поля** — проверено нажатием, слова из их `ru.json`:

  - движение товара: статус — «Проведён», «Отложен» (у меня было
    «Проведенные»/«Отложенные»); оплата — «Оплаченные», «Неоплаченные»;
    тип — их девять видов документов. В списке типов **дважды** стояли
    «Продажа» и «Возврат продажи»: у нас возврат бывает чеком и
    документом, названы они одинаково. Теперь одно название отбирает оба;
  - движение денег: статус — «Документ проведён», «Документ не проведён»
    (в деньгах он подписывает статус длиннее, чем в товаре); тип —
    «Расход», «Приход» в его порядке;
  - дата: семь его промежутков и в его порядке — сегодня, Вчера, 7 дней,
    30 дней, Этот месяц, Прошлый месяц, квартал. У меня было четыре своих.
    Год дописывается к дате только если она не из нынешнего года — правило
    его;
  - за «Фильтром» в движении товара: Автор, Отправитель, Получатель; в
    движении денег: Автор, Категория, Контрагент, Счёт.

  Чего **нет**: его полей «статус заказа» и «фискальный чек» в движении
  товара — заказов и фискальных чеков у нас нет вовсе, и эти поля не
  отбирали бы ничего. И третьего значения статуса, «Удалён»: удалённых
  документов у нас не бывает, корзины нет. «Документ не проведён» в
  деньгах честно показывает пусто — отложенных ордеров не бывает.

- [x] 02.09 · **История движения по одному товару — по его шаблону.**
  Вкладка «История движения» в карточке товара была своя: одно число со
  знаком и колонки «Магазин» и «Кто», которых у него нет. Разобрал их
  шаблон (`js/pages/card/catalog/show/blocks/history.html`) и контроллер —
  и сделал как там:

  - колонки: дата, документ, себестоимость, цена, **приход и расход
    отдельными колонками** (в одной число, в другой пусто — сразу видно,
    чего было больше), остаток после движения;
  - дата в две строки: крупно число, под ним месяц, а год двумя цифрами и
    только у прошлых лет — правило его;
  - под названием документа мелким — автор, и оба ведут: документ в сам
    документ, автор в сотрудников;
  - отбор его: дата (по умолчанию год, снять нельзя — поле `required`),
    магазин, тип, а сотрудник за кнопкой «Фильтр»;
  - листалка его: по двадцать строк, «показать еще» и «Всего документов».

  **Остаток считается, а не хранится** — суммой движений по этот момент. И
  по магазину он свой: отобрал «Черёмушки» — стоит остаток Черёмушек, а не
  общий. На это есть проверка: подписать тысячу четырнадцатью тысячами
  нельзя.

  Заодно нашлась ложь в заголовке: **любой документ, открытый по ссылке,
  подписывался «Закупка»** — в заголовке стояла подстановка по умолчанию.
  Корректировка открывалась закупкой. Теперь вид берётся из общего словаря
  видов, а когда он неизвестен, заголовок так и говорит: «документ».

- [x] 02.09 · **Свежие данные из CloudShop и расписание.** Ключ он
  присылал в переписку — программа оттуда ничего не берёт, ей нужна
  переменная окружения. Взял ключ из его сообщения, подставил в команду
  (никуда не записывая) — CloudShop принял.

  Приехало: **1 500 товаров** с полями и остатками, **3 253 клиента**,
  **46 182 чека** (9 возвратов), **3 317 корректировок**, **937 снимков** в
  288 точек.

  **Закупок у него нет вовсе.** `/documents/purchases` отдаёт ноль строк за
  все годы — проверено и с датами, и без. Склад он ведёт корректировками,
  и вот их-то перенос и не забирал: я проходил их только ради
  себестоимости, а сами документы выбрасывал. Теперь они приезжают
  документами журнала — с номером, магазином, автором и знаком количества.

  Расписание: `trig_01AMyF47ZNkk2m4eZxGPMLrs`, каждый день в 06:00 по
  Москве. Забирает новое, пересобирает файл и ссылку, проверяет журнал в
  браузере и пишет, что приехало.

  **Ключ надо перевыпустить**: он побывал в переписке, а она хранится.
  Новый — в переменные окружения под именем `CLOUDSHOP_TOKEN`, и тогда
  расписание работает само.

- [x] 02.09 · **Сверка интерфейса кассира с его кассой.** Их касса
  (`/wpapp/`) отдаёт без входа и код, и словарь подписей
  (`/locale/WPAPP/ru/…`, 458 строк) — по ним и сверял, а не по памяти.

  **Сошлось:** подпись поиска слово в слово («Поиск по наименованию,
  артикулу, штрихкоду, коду и описанию» — их `SEARCH_CATALOG`); «Очередь
  чеков» и «Непроведенные» (`DOC_PARKED`, `HOLD_SALES_SHORT`); «Смена
  закрыта», «Открыть смену», «Сверка денег в кассе», «Сумма денег в кассе»;
  окно оплаты — «Принять оплату», «Наличные», «Безналичные», «Отсрочка»,
  «Варианты оплаты наличными», «Итог», «Подытог»; меню целиком, вплоть до
  красного «Закрыть смену» и карточки пользователя сверху; три вида витрины
  — товары, категории, группы — и шестерёнка рекомендаций справа.

  **Не сходилось — три клавиши, теперь есть:**

  - **Пробел** — оплата. У него это `HC("Space")` прямо у кнопки продажи;
  - **Shift** — открывает меню. У нас Shift показывал цифры у пунктов, но
    само меню им не открывалось: цифры были, а войти в меню тем же пальцем
    нельзя;
  - **Escape** — выбор витрины (товары / категории / группы). У него это
    `case "Escape"` у поля поиска, тот же список, что под синим значком.

  Проверено нажатием в собранной кассе: пробел открывает оплату, Shift —
  меню, Escape — выбор витрины.

  Чего у нас нет и не будет: пункта «Вернуться к старой версии». Старой
  версии у нас не было, и Вазген это подтвердил: «нам не нужна старая
  версия кассы, её не будет». Вопрос закрыт — возвращаться к нему не надо.

## Осталось

- [ ] Чтение веса с весов на кассу — [?] нужно знать, как весы подключены
  (COM-порт, сеть) и какой у них протокол. Выгрузка файла для весов
  сделана, а вот приём веса — это уже разговор с железом.
- [ ] Ручной подбор товаров в списки рекомендаций.
- [ ] Себестоимость продаж сходится с кабинетом только по порядку величины
  (41 278,00 против 32 979,88): кабинет фиксирует её на момент чека, а
  перенос знает только нынешнюю из карточки. Если понадобится точь-в-точь —
  можно собрать из тех же корректировок ленту «когда какая себестоимость
  была» и считать каждый старый чек по ней.
